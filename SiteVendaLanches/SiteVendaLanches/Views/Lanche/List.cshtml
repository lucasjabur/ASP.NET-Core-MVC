@model LancheListViewModel

@* <div>
    <h2>@ViewData["Título"]</h2>
</div>
<h2>@ViewData["Data"]</h2>
<hr />
<h3>@ViewBag.Total @ViewBag.NumLanches</h3>
<hr /> *@

@* <h3>@TempData["Nome"]</h3> *@

<div>
    <h2>@Model.CategoriaAtual</h2>
    <hr />
</div>

<div class="container">
    <div class="row">
        @foreach (var lanche in Model.Lanches) {
            <partial name="_LanchesResumo" for="@lanche" />
        }
    </div>
</div>
    